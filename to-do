-go through all sinks&sources&sanitizers to see if all are covered

-add validation through comments: regex match

-check for tainted and clear variables in global scope - across blocks

-automate on multiple files (or even directories?)
    --done
	make --skip-hidden a parameter that is true by default

-color and beautify output
